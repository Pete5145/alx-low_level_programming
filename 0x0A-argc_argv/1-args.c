#include <stdio.h>

/**
 * main - starting of the program, followed by a newline.
 * @argc: number of arguments passed to the program.
 * @argv: array of strings passed to the program.
 *
 * Return: Always (0).
 */
int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc);

	return (0);
}
