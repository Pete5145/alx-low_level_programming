#include "main.h"

/**
 * _strlen - Returns the length of a string.
 * @str: string
 *
 * @Return: Length.
 */
int size_t_strlen(const char *str)
{
	int size_t_len = 0;

	while (*str++)
		len++;

	return (len);
}
