#include <stdio.h>
#include <ctype.h>

/**
 *
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	 int alphabet;
	 scanf("%c", alphabet);
	 putschar(tolower(alphabet);
	 
	 return (0);
}
