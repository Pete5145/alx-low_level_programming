#include <stdio.h>
#include <ctype.h>

/**
 *
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	 char alphabet;

	 scanf("%c", alphabet);
	 putchar(tolower(alphabet);
	return (0);
}
